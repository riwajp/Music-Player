#include <SFML/Graphics.hpp>

/*

int main(){
	
	
	sf::Window window(sf::VideoMode(800,800),"Music Player");
	sf::Event event;
	while(window.isOpen()){
	while(window.pollEvent(event)){
		
		if(event.type==sf::Event::Closed) {
			window.close();
		}
		
		
	}
}
}

*/
